apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-app
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      type: frontend
      service: core-app
  template:
    metadata:
      labels:
        type: frontend
        service: core-app
    spec:
      containers:
        - name: container
          image: sellerspot/core-app
          imagePullPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: core-app
  namespace: app
spec:
  type: NodePort
  ports:
    - port: 3000
  selector:
    type: frontend
    service: core-app
